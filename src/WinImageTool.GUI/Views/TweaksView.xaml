<UserControl x:Class="Cleanse11.Views.TweaksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Tweak Groups -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="⚡ Performance Tweaks" Style="{StaticResource HeadingAccentStyle}" Margin="0,0,0,4"/>
                <TextBlock Grid.Row="1" Text="Apply registry tweaks to optimize Windows 11. Click a group to expand and see individual changes."
                           Style="{StaticResource MutedTextStyle}" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <Border Grid.Row="2" Style="{StaticResource InfoBannerStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💡" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Changes are applied to the CURRENT system, not the mounted image. Restart recommended after applying."
                                   VerticalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Action Buttons -->
                <Border Grid.Row="3" Background="{StaticResource BackgroundBrush}" 
                        CornerRadius="8" Padding="4" Margin="0,14,0,14">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Select All" Command="{Binding SelectAllCommand}" 
                                Padding="16,8" FontSize="12"/>
                        <Button Content="Clear" Command="{Binding ClearAllCommand}" 
                                Margin="6,0,0,0" Padding="16,8" FontSize="12"/>
                        <Border Width="1" Background="{StaticResource BorderBrush}" 
                                Margin="12,4"/>
                        <Button Content="Apply Selected" Command="{Binding ApplyCommand}"
                                Style="{StaticResource PrimaryButtonStyle}" 
                                Padding="18,8" FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- Tweak Groups List -->
                <ScrollViewer Grid.Row="4" Style="{StaticResource FadeScrollBarScrollViewer}" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding TweakGroups}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BackgroundLighterBrush}"
                                        CornerRadius="10" Margin="0,0,0,10" Padding="0"
                                        BorderBrush="{StaticResource PinkLightBrush}" BorderThickness="1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Header - Clickable to expand -->
                                        <Border Grid.Row="0" Background="Transparent" Cursor="Hand"
                                                MouseLeftButtonDown="ToggleExpand">
                                            <Grid Margin="12,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" 
                                                          VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}" 
                                                           FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                    <Border Background="{StaticResource BackgroundBrush}" 
                                                            CornerRadius="6" Padding="8,3" VerticalAlignment="Center" Margin="0,0,8,0">
                                                        <TextBlock Text="{Binding EntryCount, StringFormat='{}{0} changes'}"
                                                                   Foreground="{StaticResource AccentBrush}" FontSize="11"/>
                                                    </Border>
                                                    <TextBlock Text="{Binding ExpandIcon}" FontSize="12" 
                                                               Foreground="{StaticResource ForegroundMutedBrush}"
                                                               VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                        <!-- Description -->
                                        <TextBlock Grid.Row="1" Text="{Binding Description}" 
                                                   Foreground="{StaticResource ForegroundMutedBrush}" 
                                                   FontSize="12" TextWrapping="Wrap" Margin="42,0,12,8"/>

                                        <!-- Expanded Details -->
                                        <Border Grid.Row="1" Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}"
                                                Background="{StaticResource BackgroundBrush}" CornerRadius="6" Margin="12,28,12,10" Padding="10">
                                            <StackPanel>
                                                <TextBlock Text="Registry Changes:" FontWeight="SemiBold" FontSize="11" 
                                                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>
                                                <ItemsControl ItemsSource="{Binding Entries}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" 
                                                                    Padding="0,4" Margin="0,0,0,2">
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition Width="*"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" Margin="0,0,8,0"/>
                                                                    <StackPanel Grid.Column="1">
                                                                        <TextBlock Text="{Binding Description}" FontSize="11" FontWeight="Medium" TextWrapping="Wrap"/>
                                                                        <TextBlock Text="{Binding Path}" FontSize="10" 
                                                                                   Foreground="{StaticResource ForegroundMutedBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                                            <TextBlock Text="{Binding ValueName}" FontSize="10" 
                                                                                       Foreground="{StaticResource PinkLightBrush}" FontWeight="Medium"/>
                                                                            <TextBlock Text=" = " FontSize="10" Foreground="{StaticResource ForegroundMutedBrush}"/>
                                                                            <TextBlock Text="{Binding ValueDisplay}" FontSize="10" 
                                                                                       Foreground="{StaticResource ForegroundMutedBrush}"/>
                                                                        </StackPanel>
                                                                    </StackPanel>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Right Panel - Log -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="📋 Log" Style="{StaticResource SubheadingStyle}"/>
                    <Button Content="Clear" Command="{Binding ClearLogCommand}" 
                            Margin="12,0,0,0" Padding="10,4"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" Style="{StaticResource FadeScrollBarScrollViewer}" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Log}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="11"
                                           Foreground="{StaticResource ForegroundBrush}"
                                           TextWrapping="Wrap" Margin="0,0,0,4"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock Grid.Row="2" Text="{Binding Status}" Margin="0,10,0,0"
                           Foreground="{StaticResource AccentBrush}" FontWeight="Medium" TextWrapping="Wrap"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
